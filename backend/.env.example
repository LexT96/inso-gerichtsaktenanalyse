
# OpenAI / GPT-5.4 (alternative extraction provider)
# Set EXTRACTION_PROVIDER=openai to use GPT instead of Claude for Stage 2
# EXTRACTION_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-5.4
# OPENAI_BASE_URL=        # For Azure OpenAI: https://{resource}.openai.azure.com/openai/deployments/{deployment}
